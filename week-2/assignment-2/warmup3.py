"""Warmup 3 - First git commit, plus a note about what I learned this week.

Committing warmup1 and warmup2 first is what made the git log below exist -
there was nothing to show until something had actually been committed.
"""

# git log --oneline output:
# 2f9b361 Add warmup1 and warmup2 for week 2
# 294742d add week 2 folder
# 58d836d Add files via upload
#
# Reading that: newest at the top, oldest at the bottom. The short string on
# the left is the commit hash, an id for that snapshot, which is how I'd point
# at a specific commit later. The bottom two came with the repo when I forked
# it, so they aren't mine. --oneline just squashes each commit down to one row
# instead of printing the author and date for every one.

print("This week I learned to move around my computer from the terminal")
print("and to save my work in Git with branches, commits, and a pull request.")
